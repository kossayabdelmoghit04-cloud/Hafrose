/**
 * HAFROSE Component Library — Layout Category
 *
 * Responsibility: Structural grid and spacing components for page assembly.
 *
 * Members:
 *   Container     — Max-width responsive content wrapper
 *   Grid          — CSS Grid utility with responsive column control
 *   Section       — Semantic page section with consistent vertical rhythm
 *   Flex          — Flexbox utility wrapper
 *   Stack         — Vertical/Horizontal stacking with gap
 *
 * Import rule: pages → import from '@components/layout'
 */
export {};
