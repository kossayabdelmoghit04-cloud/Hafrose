/**
 * HAFROSE Component Library — UI Category
 *
 * Responsibility: Primitive, brand-styled building blocks.
 * These components have NO business logic, only visual presentation.
 *
 * Members:
 *   Button      — Primary, Secondary, Ghost, Destructive variants
 *   Badge       — Status, Category, Promo badges
 *   Divider     — Luxury ornamental dividers
 *   Spinner     — Loading indicators
 *   Tag         — Label chips (for filters, categories)
 *   Avatar      — User profile image with fallback initials
 *
 * Naming rule: PascalCase, suffixed per category (e.g. no suffix for ui/)
 * Import rule: pages/features → import from '@components/ui'
 */
export {};
