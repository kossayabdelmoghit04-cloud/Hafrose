/**
 * HAFROSE Component Library — Common Category
 *
 * Responsibility: Reusable cross-feature components with mild business awareness.
 * These components may consume stores and hooks but never feature-specific state.
 *
 * Members:
 *   PageTitle       — Consistent page hero title section
 *   SectionHeader   — Section title + subtitle block
 *   EmptyState      — Illustrated empty content placeholder
 *   ErrorState      — Graceful error display with retry CTA
 *   PageLoader      — Full-page loading overlay
 *   Breadcrumbs     — Navigation trail
 *   LazyImage       — Optimized progressive image with blur-up
 *
 * Import rule: features/pages → import from '@components/common'
 */
export {};
