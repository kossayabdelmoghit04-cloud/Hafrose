/**
 * HAFROSE Component Library — Feedback Category
 *
 * Responsibility: User feedback and notification components.
 *
 * Members:
 *   Toast         — Non-blocking notification messages (success, error, info)
 *   Modal         — Accessible dialog overlay
 *   Drawer        — Slide-over panel (cart, filters, mobile menu)
 *   Alert         — Inline warning/info/success blocks
 *   ConfirmDialog — Destructive action confirmation dialog
 *   Skeleton      — Shimmer loading placeholder
 *
 * Import rule: Features/layouts → import from '@components/feedback'
 */
export {};
