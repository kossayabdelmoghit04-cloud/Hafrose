/**
 * HAFROSE Component Library — Navigation Category
 *
 * Responsibility: Application navigation structures.
 *
 * Members:
 *   Navbar          — Main luxury header with cart + wishlist indicators
 *   MobileMenu      — Hamburger-triggered slide-over navigation
 *   Footer          — Multi-column luxury footer
 *   SearchBar       — Full-screen search overlay
 *   CategoryTabs    — Horizontal scrollable category filter bar
 *   Pagination      — Page navigation for catalog
 *   AccountSidebar  — Customer account section navigation
 *   AdminSidebar    — Back-office management navigation
 *
 * Import rule: Layouts → import from '@components/navigation'
 */
export {};
